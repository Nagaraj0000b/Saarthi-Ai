---
phase: 03-filtering-documentation
plan: 01
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: ["ml_engine/main.py", "ml_engine/test_api.py"]
autonomous: true
requirements: [ML-04, DOCS-01]
must_haves:
  truths:
    - "API only returns jobs that the user is registered for."
    - "API only returns jobs that are compatible with user's skills."
    - "All ML engine files have comprehensive docstrings."
  artifacts:
    - path: "ml_engine/main.py"
      provides: "Filtered job recommendations"
    - path: "ml_engine/test_api.py"
      provides: "Tests for filtered recommendations"
---

<objective>
Implement job filtering logic in the inference API to only return recommendations for jobs the user is registered for and is compatible with (ML-04). Verify that documentation requirements (DOCS-01) are satisfied.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API Schemas and Service Logic</name>
  <files>ml_engine/main.py</files>
  <action>
    Modify `ml_engine/main.py`:
    1. Import `List` from `typing` if not present.
    2. Add `registered_jobs: List[str] = []` to the `UserContext` schema.
    3. In `get_recommendations`, loop through `JOB_TO_TYPE.keys()` but *only* predict and append jobs that exist in `context.registered_jobs` (if it is provided). If a job is compatible (`result.is_compatible == True`), include it in the final list.
    4. Ensure the returned list is sorted by `predicted_earning`.
  </action>
  <acceptance_criteria>
    - `UserContext` accepts `registered_jobs`
    - Incompatible jobs are filtered out from the response.
    - Unregistered jobs are filtered out.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update Tests for Filtering</name>
  <files>ml_engine/test_api.py</files>
  <action>
    Update `ml_engine/test_api.py` to pass `registered_jobs` in the payload for Test 2, Test 3, and Test 4. Ensure it verifies that only registered jobs are returned.
  </action>
  <acceptance_criteria>
    - Tests complete without errors and demonstrate filtering.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify DOCS-01 Compliance</name>
  <action>
    Verify that all Python files in `ml_engine/` have the required docstrings and plain English definitions for technical terms. This was completed in the previous step, so just mark DOCS-01 as verified.
  </action>
  <acceptance_criteria>
    - All functions have docstrings.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `python ml_engine/test_api.py` and manually verify that no jobs outside of the `registered_jobs` array are recommended, and no `is_compatible=False` jobs are returned.
</verification>

<success_criteria>
- ML-04 is satisfied by the filtering logic in `main.py`.
- DOCS-01 is verified.
- API tests run successfully.
</success_criteria>
