---
phase: 04-backend-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [server/services/jobRecommendationService.js]
autonomous: true
requirements: [INT-02, INT-03]

must_haves:
  truths:
    - "Recommendation service fetches missing user skills/jobs from MongoDB"
    - "Service passes complete user context to the ML engine API"
    - "Fallback logic uses jobMapping.js configuration"
    - "Human-readable reasoning is category-aware"
  artifacts:
    - path: "server/services/jobRecommendationService.js"
      provides: "Smart recommendation logic with ML integration"
  key_links:
    - from: "server/services/jobRecommendationService.js"
      to: "server/models/User.js"
      via: "User.findById"
    - from: "server/services/jobRecommendationService.js"
      to: "server/config/jobMapping.js"
      via: "import and reference"
---

<objective>
Update the job recommendation service to dynamically fetch user data from the database and refine the ML-based reasoning and fallback logic using the centralized job mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-backend-integration/04-01-SUMMARY.md
@server/services/jobRecommendationService.js
@server/config/jobMapping.js
@server/models/User.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic User Context for ML API</name>
  <files>server/services/jobRecommendationService.js</files>
  <action>
    - Update `recommendJobs` to check if `skills` or `preferredJobs` (for `registered_jobs`) are provided.
    - If not provided, fetch the `User` document from the database using `userId` per D-02.
    - Map the database/argument skill names to the ML-required flags (skill_bike, skill_car, etc.).
    - Update the `mlContext` payload to include the fetched/provided `registered_jobs` (passed as `registered_jobs` to ML API).
    - Ensure the ML API call uses the complete, enriched context.
  </action>
  <verify>
    <automated>grep -q "User.findById" server/services/jobRecommendationService.js && grep -q "registered_jobs: " server/services/jobRecommendationService.js</automated>
  </verify>
  <done>The recommendation service automatically retrieves missing user profile data from the DB before calling the ML engine.</done>
</task>

<task type="auto">
  <name>Task 2: Centralized Logic and Reasoning Refinement</name>
  <files>server/services/jobRecommendationService.js</files>
  <action>
    - Import `jobMapping.js` into `server/services/jobRecommendationService.js`.
    - Update `fallbackRuleEngine` to use `jobMapping.js` platform lists instead of hardcoded `JOBS_BY_SKILL`.
    - Refine `generateMLReason` to use category information from `jobMapping.js` to provide more context-aware explanations per D-04.
    - Ensure `predicted_earning` and `is_compatible` flags from ML API correctly influence the final output.
  </action>
  <verify>
    <automated>grep -q "require('../config/jobMapping')" server/services/jobRecommendationService.js</automated>
  </verify>
  <done>Service logic is decoupled from platform lists, and reasoning text is dynamically generated based on centralized job categories.</done>
</task>

</tasks>

<success_criteria>
1. `recommendJobs` fetches `User` data if parameters are missing.
2. The ML API receives `registered_jobs` and skill flags derived from the user profile.
3. `fallbackRuleEngine` correctly uses the 5 categories from `jobMapping.js`.
4. The service returns ranked results filtered by user preferences and compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-integration/04-02-SUMMARY.md`
</output>
