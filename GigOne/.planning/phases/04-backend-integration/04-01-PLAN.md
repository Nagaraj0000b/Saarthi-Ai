---
phase: 04-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/models/User.js, server/config/jobMapping.js, server/routes/auth.js, server/controllers/authController.js, scripts/reset_users.js]
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "User model persists skills and registeredJobs arrays"
    - "New profile endpoint allows updating user skills/jobs"
    - "Database can be reset to a clean state with a test user"
    - "Job categories are centrally mapped in server config"
  artifacts:
    - path: "server/models/User.js"
      provides: "Updated schema for skills and jobs"
    - path: "server/config/jobMapping.js"
      provides: "Unified job category and platform mapping"
    - path: "server/routes/auth.js"
      provides: "PATCH /profile endpoint"
  key_links:
    - from: "server/controllers/authController.js"
      to: "server/models/User.js"
      via: "User.findByIdAndUpdate"
---

<objective>
Establish the backend foundation for user-specific data by updating the User model, creating a centralized job mapping configuration, and providing a profile synchronization endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-backend-integration/04-CONTEXT.md
@server/models/User.js
@server/routes/auth.js
@server/controllers/authController.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update User Model & Setup Job Mapping</name>
  <files>server/models/User.js, server/config/jobMapping.js</files>
  <action>
    - Update `server/models/User.js` to include `skills: [String]` and `registeredJobs: [String]` with default `[]`.
    - Create `server/config/jobMapping.js` based on `ml_engine/main.py` constants.
    - Include 5 categories: "Ride hailing drivers (cab+ bike)", "Ride hailing instant delivery", "Delivery workers in General", "On Demand home based services", "Remote service providers".
    - Map each category to its respective platforms (Uber, Swiggy, Zomato, etc.) per D-04.
  </action>
  <verify>
    <automated>node -e "const User = require('./server/models/User'); console.log(User.schema.paths.skills ? 'OK' : 'FAIL')"</automated>
  </verify>
  <done>User schema updated and jobMapping.js created with correct category-to-platform links.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Profile Sync Endpoint</name>
  <files>server/routes/auth.js, server/controllers/authController.js</files>
  <action>
    - Add `updateProfile` controller in `server/controllers/authController.js` that uses `User.findByIdAndUpdate` to save `name`, `skills`, and `registeredJobs`.
    - Ensure it uses the authenticated user ID from `req.user.userId`.
    - Add `PATCH /profile` route in `server/routes/auth.js` using the existing auth middleware (verifyToken or similar if available, otherwise just implement the logic) per D-03.
    - Note: Ensure validation allows empty arrays for reset scenarios.
  </action>
  <verify>
    <automated>grep -q "router.patch('/profile'" server/routes/auth.js && grep -q "updateProfile =" server/controllers/authController.js</automated>
  </verify>
  <done>Authenticated users can update their skills and job registrations via the PATCH /api/auth/profile endpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Create Data Reset Script</name>
  <files>scripts/reset_users.js</files>
  <action>
    - Create `scripts/reset_users.js` that connects to MongoDB using `server/config/db.js`.
    - Clear the `users` collection.
    - Create a test user: `test@test.com`, password `password123`, with empty `skills` and `registeredJobs` per D-01.
    - Close the connection after completion.
  </action>
  <verify>
    <automated>node scripts/reset_users.js && node -e "const User = require('./server/models/User'); require('./server/config/db')().then(() => User.findOne({email: 'test@test.com'}).then(u => { console.log(u ? 'RESET_OK' : 'RESET_FAIL'); process.exit(0); }))"</automated>
  </verify>
  <done>A standalone script exists to reset the database and prepare a fresh test user for integration testing.</done>
</task>

</tasks>

<success_criteria>
1. `User` model supports `skills` and `registeredJobs`.
2. `server/config/jobMapping.js` exists with all platforms mapped to 5 categories.
3. `PATCH /api/auth/profile` successfully updates a user's persistent data in MongoDB.
4. `scripts/reset_users.js` can be executed to clear data and seed the test user.
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-integration/04-01-SUMMARY.md`
</output>
