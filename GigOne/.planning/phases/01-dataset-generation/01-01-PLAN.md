---
phase: 01-dataset-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["ml_engine/generate_dataset.py"]
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Dataset script generates exactly 50,000 rows"
    - "Dataset contains only 5 job categories with no 'Other' category"
    - "Each of the 5 categories has exactly 20% (10,000) of the total rows"
    - "Remote service providers' target_earning is never modified by weather or traffic factors"
  artifacts:
    - path: "ml_engine/generate_dataset.py"
      provides: "Dataset generation script"
      contains: "TOTAL_ROWS = 50000"
    - path: "ml_engine/data/synthetic_earnings.csv"
      provides: "Generated dataset"
  key_links:
    - from: "ml_engine/generate_dataset.py"
      to: "ml_engine/data/synthetic_earnings.csv"
      via: "csv export"
      pattern: "to_csv"
---

<objective>
Update the dataset generation script to produce 50,000 rows with a perfectly balanced distribution across 5 job categories, and strictly enforce zero environmental impact for remote/freelance jobs.

Purpose: Generate the unbiased dataset (DATA-01) with correct environmental constraints (DATA-02) required for retraining the XGBoost model in Phase 2.
Output: Updated `ml_engine/generate_dataset.py` capable of producing `synthetic_earnings.csv` meeting all constraints.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-dataset-generation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dataset Configuration and Distribution Logic</name>
  <files>ml_engine/generate_dataset.py</files>
  <read_first>
    - ml_engine/generate_dataset.py
  </read_first>
  <action>
    Modify `ml_engine/generate_dataset.py` to enforce the new dataset rules per D-01 and D-02:
    1. Update the constant `TOTAL_ROWS = 50000`.
    2. Remove the "Other" category entirely. Delete `"Other"` from `ALL_JOBS`, `JOB_TO_TYPE`, `BASE_RATES`, and `COMPATIBILITY`. Ensure `JOB_TYPES` remains exactly 5 keys.
    3. Rewrite the row generation loop in `main()`. Instead of `rows_per_job = TOTAL_ROWS // len(ALL_JOBS)`, implement logic that allocates exactly 10,000 rows to each of the 5 categories in `JOB_TYPES` (20% of 50,000). For each category, divide the 10,000 rows equally among its jobs (e.g., if a category has 4 jobs, each job gets 2,500 rows).
    4. Distribute any remainder rows within the category to hit exactly 10,000 per category.
    5. Maintain the `COLD_START_RATIO = 0.15` logic (15% of rows for each job should use `is_cold_start=True`).
    6. Update `.planning/STATE.md` after task completion to reflect the implemented changes.
  </action>
  <acceptance_criteria>
    - `grep -q "TOTAL_ROWS = 50000" ml_engine/generate_dataset.py` exits 0
    - `grep -q '"Other"' ml_engine/generate_dataset.py` exits 1
    - `python ml_engine/generate_dataset.py` executes successfully
  </acceptance_criteria>
  <verify>
    <automated>python ml_engine/generate_dataset.py</automated>
  </verify>
  <done>Dataset runs successfully, prints "Total Rows: 50000", and no longer includes "Other".</done>
</task>

<task type="auto">
  <name>Task 2: Enforce Zero Environmental Impact for Remote Jobs</name>
  <files>ml_engine/generate_dataset.py</files>
  <read_first>
    - ml_engine/generate_dataset.py
  </read_first>
  <action>
    Modify the `generate_row` function in `ml_engine/generate_dataset.py` to explicitly fulfill D-03:
    1. Locate the "Weather Impact" and "Traffic Impact" sections.
    2. Add an explicit condition so that weather and traffic multipliers are bypassed for "Remote service providers".
    3. Wrap the weather and traffic modifier logic in an `if job_type != "Remote service providers":` block.
    4. Ensure the base rate, peak hours, weekend effect, burnout, and noise factors for these jobs are still applied appropriately. Only nullify weather and traffic impacts.
    5. Update `.planning/STATE.md` after task completion to reflect the implemented changes.
  </action>
  <acceptance_criteria>
    - `grep -q 'job_type != "Remote service providers"' ml_engine/generate_dataset.py` exits 0 OR `grep -q 'if job_type == "Remote service providers":' ml_engine/generate_dataset.py` exits 0
    - `python ml_engine/generate_dataset.py` executes without syntax errors
  </acceptance_criteria>
  <verify>
    <automated>python ml_engine/generate_dataset.py</automated>
  </verify>
  <done>Remote service providers have exactly zero deviation applied from weather or traffic factors.</done>
</task>

</tasks>

<verification>
Run `python ml_engine/generate_dataset.py` and manually verify the console output:
- Total Rows should be exactly 50000.
- The 5 job type categories should each have exactly 10000 rows.
</verification>

<success_criteria>
- Dataset generation script runs without errors and produces `data/synthetic_earnings.csv`.
- The dataset exactly contains 50,000 rows and 5 categories without 'Other'.
- Environmental factors explicitly do not impact the base rate for "Remote service providers".
</success_criteria>

<output>
After completion, create `.planning/phases/01-dataset-generation/01-01-SUMMARY.md`
</output>