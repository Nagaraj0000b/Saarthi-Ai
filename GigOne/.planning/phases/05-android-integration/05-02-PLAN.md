---
phase: 05-android-integration
plan: 05-02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - android/app/src/main/java/com/gigone/saarthi/ui/screens/DashboardScreen.kt
  - android/app/src/main/java/com/gigone/saarthi/ui/screens/RecommendationListScreen.kt
autonomous: true
requirements: [INT-05, INT-06]
must_haves:
  truths:
    - "Dashboard recommendation card displays the job category (jobType) under the job name."
    - "Recommendation list screen displays the job category for all listed jobs."
  artifacts:
    - path: "android/app/src/main/java/com/gigone/saarthi/ui/screens/DashboardScreen.kt"
      provides: "Updated recommendation card with jobType"
    - path: "android/app/src/main/java/com/gigone/saarthi/ui/screens/RecommendationListScreen.kt"
      provides: "Updated list items with jobType"
  key_links:
    - from: "android/app/src/main/java/com/gigone/saarthi/ui/screens/DashboardScreen.kt"
      to: "android/app/src/main/java/com/gigone/saarthi/data/Models.kt"
      via: "Recommendation model jobType field"
---

<objective>
Update the Android UI to display the new ML-based job categories (jobType) in the Dashboard and Recommendation List screens.

Purpose: Provide users with better clarity on the nature of recommended jobs based on the new 5-tier classification.
Output: Enhanced UI displaying job categories for all recommendations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-android-integration/05-01-PLAN.md

<interfaces>
From android/app/src/main/java/com/gigone/saarthi/data/Models.kt (updated in 05-01):
```kotlin
data class Recommendation(
    val job: String,
    val finalScore: Float,
    val reason: String = "",
    val jobType: String? = null,
    // ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dashboard and Recommendation List UI</name>
  <files>
    android/app/src/main/java/com/gigone/saarthi/ui/screens/DashboardScreen.kt,
    android/app/src/main/java/com/gigone/saarthi/ui/screens/RecommendationListScreen.kt
  </files>
  <action>
    - Update `DashboardScreen.kt` recommendation card: Display `topJob.jobType` in a small pill or as text under the job name.
    - Update `RecommendationListScreen.kt` list items: Display `jobItem.jobType` for each job in the list.
    - Use appropriate colors from `AppColors` for the category labels.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>Job categories (jobType) are now displayed in both the Dashboard and the Recommendation list.</done>
</task>

</tasks>

<verification>
1. Open the app and navigate to the Dashboard.
2. Verify the recommendation card shows the category (e.g., "Ride hailing & instant delivery").
3. Click "Best Job Assignments" and verify all jobs in the list show their respective categories.
</verification>

<success_criteria>
- The new 5-tier job classification is visible to the user in the UI.
- Categories are displayed consistently across different screens.
</success_criteria>

<output>
After completion, create `.planning/phases/05-android-integration/05-02-SUMMARY.md`
</output>
