---
phase: 05-android-integration
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gigone/saarthi/data/Models.kt
  - android/app/src/main/java/com/gigone/saarthi/data/AuthApi.kt
  - android/app/src/main/java/com/gigone/saarthi/util/TokenManager.kt
  - android/app/src/main/java/com/gigone/saarthi/ui/screens/SignInScreen.kt
  - android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileViewModel.kt
  - android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileSubScreens.kt
autonomous: true
requirements: [INT-04]
must_haves:
  truths:
    - "Changing skills or jobs in the management screens triggers a backend sync."
    - "Logging in with an existing user loads their skills and jobs from the backend into the app."
    - "Management screens show a loading indicator while syncing with the backend."
    - "Errors during profile sync are displayed to the user via Snackbar or Toast."
  artifacts:
    - path: "android/app/src/main/java/com/gigone/saarthi/data/Models.kt"
      provides: "Updated UserData with skills and registeredJobs"
    - path: "android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileViewModel.kt"
      provides: "ViewModel for handling profile synchronization"
  key_links:
    - from: "android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileSubScreens.kt"
      to: "android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileViewModel.kt"
      via: "ViewModel instance"
    - from: "android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileViewModel.kt"
      to: "android/app/src/main/java/com/gigone/saarthi/data/AuthApi.kt"
      via: "updateProfile API call"
---

<objective>
Update the Android app's data layer and profile management screens to synchronize user skills and job registrations with the backend.

Purpose: Ensure that user preferences are persisted server-side, enabling personalized ML recommendations across devices.
Output: Integrated profile synchronization logic in the Android app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-android-integration/05-CONTEXT.md

<interfaces>
From android/app/src/main/java/com/gigone/saarthi/data/Models.kt:
```kotlin
data class UserData(val id: String, val name: String, val role: String)
data class AuthResponse(val token: String, val user: UserData)
```

From android/app/src/main/java/com/gigone/saarthi/data/AuthApi.kt:
```kotlin
interface AuthApi {
    @POST("auth/login")
    suspend fun login(@Body request: LoginRequest): AuthResponse
}
```

From android/app/src/main/java/com/gigone/saarthi/util/TokenManager.kt:
```kotlin
fun saveSkills(ctx: Context, skills: Set<String>)
fun saveJobs(ctx: Context, jobs: Set<String>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Data Models and Auth API</name>
  <files>
    android/app/src/main/java/com/gigone/saarthi/data/Models.kt,
    android/app/src/main/java/com/gigone/saarthi/data/AuthApi.kt
  </files>
  <action>
    - Update `UserData` in `Models.kt` to include `val skills: List<String> = emptyList()` and `val registeredJobs: List<String> = emptyList()`.
    - Update `AuthApi.kt` to include a new endpoint: `@PATCH("auth/profile") suspend fun updateProfile(@Body profile: Map<String, Any>): Map<String, Any>`.
    - This ensures the app can receive and send the new profile fields.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>Models and API interface updated to support profile synchronization.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Profile ViewModel and Login Sync</name>
  <files>
    android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileViewModel.kt,
    android/app/src/main/java/com/gigone/saarthi/ui/screens/SignInScreen.kt
  </files>
  <action>
    - Create `ProfileViewModel.kt` to manage profile updates. It should have a `syncProfile(skills: Set<String>, jobs: Set<String>)` function that calls `AuthApi.updateProfile`.
    - Use `StateFlow` to track `isLoading`, `isSuccess`, and `errorMessage`.
    - Upon successful sync, update `TokenManager` with the new skills and jobs.
    - Update `SignInScreen.kt` to extract `skills` and `registeredJobs` from the `AuthResponse` and save them to `TokenManager` using `saveSkills` and `saveJobs`.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>ViewModel created and login flow updated to sync profile data from the backend.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Sync Logic into Management Screens</name>
  <files>
    android/app/src/main/java/com/gigone/saarthi/ui/screens/ProfileSubScreens.kt
  </files>
  <action>
    - Update `ManageJobsScreen` and `ManageSkillsScreen` in `ProfileSubScreens.kt` to use `ProfileViewModel`.
    - In the "Back" button click or explicit "Save" action (if added), trigger `vm.syncProfile(...)`.
    - Display a `CircularProgressIndicator` or similar loading overlay when `vm.isLoading` is true.
    - Show a `Toast` or `Snackbar` upon success or failure of the sync operation.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <done>Management screens now synchronize changes with the backend via ProfileViewModel.</done>
</task>

</tasks>

<verification>
1. Log in and verify (via logcat or debugger) that initial skills/jobs are loaded from the backend.
2. Modify skills in the app, save, and verify the backend `PATCH` request is sent with correct data.
3. Verify the loading indicator appears during sync and a success message appears after.
</verification>

<success_criteria>
- User profile (skills/jobs) is synchronized between the Android app and the backend.
- UI provides feedback during and after synchronization.
- Backend skills/jobs are correctly cached in TokenManager after login.
</success_criteria>

<output>
After completion, create `.planning/phases/05-android-integration/05-01-SUMMARY.md`
</output>
