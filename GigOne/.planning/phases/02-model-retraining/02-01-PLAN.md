---
phase: 02-model-retraining
plan: 01
type: execute
wave: 1
depends_on: ["01-01"]
files_modified: ["ml_engine/models/*"]
autonomous: true
requirements: [ML-01, ML-02, ML-03]
must_haves:
  truths:
    - "Model achieved R² score > 0.90"
    - "SHAP summary plot generated and saved"
    - "Label encoders and metadata saved correctly"
  artifacts:
    - path: "ml_engine/models/gigone_xgb_model.json"
      provides: "Retrained XGBoost model"
    - path: "ml_engine/models/shap_summary.png"
      provides: "Explainability proof"
---

<objective>
Retrain the XGBoost model using the updated 50k dataset and verify the environmental decoupling logic through metrics and SHAP analysis.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-model-retraining/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute Model Training</name>
  <files>ml_engine/train_model.py</files>
  <action>
    Run the training script to process the 50,000-row dataset and generate the model artifacts.
  </action>
  <acceptance_criteria>
    - `python ml_engine/train_model.py` exits 0
    - Console output shows R² > 0.90
  </acceptance_criteria>
  <verify>
    <automated>python ml_engine/train_model.py</automated>
  </verify>
  <done>All model artifacts exported successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Environmental Decoupling via Metrics</name>
  <files>ml_engine/models/model_metadata.json</files>
  <action>
    Check the generated `model_metadata.json` to confirm metrics are within acceptable range.
  </action>
  <acceptance_criteria>
    - Metrics in `model_metadata.json` show r2 > 0.90
  </acceptance_criteria>
  <done>Metrics confirmed.</done>
</task>

</tasks>

<verification>
Check `ml_engine/models/` for existence of all 4 artifacts and verify the console output of the training script.
</verification>

<success_criteria>
- Model trained and saved to `ml_engine/models/gigone_xgb_model.json`.
- Encoders and metadata updated.
- R² score is above 0.90.
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-retraining/02-01-SUMMARY.md`
</output>