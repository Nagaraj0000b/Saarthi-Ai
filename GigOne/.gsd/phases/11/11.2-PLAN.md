---
phase: 11
plan: 2
wave: 1
---

# Plan 11.2: Simplify Shift History (Minimalist Card Design)

## Objective
Simplify the Work Logs (Shift History) screen based on user feedback. Move away from the complex "Timeline" view to a cleaner, grouped list with minimalist cards that are easier to read and scan.

## Context
- `android/app/src/main/java/com/gigone/saarthi/ui/screens/WorkLogsScreen.kt`
- `android/app/src/main/java/com/gigone/saarthi/data/Models.kt`

## Tasks

<task type="auto">
  <name>Implement Grouped Date Headers</name>
  <files>android/app/src/main/java/com/gigone/saarthi/ui/screens/WorkLogsScreen.kt</files>
  <action>
    - Refactor the list to group logs by their date (Today, Yesterday, Date).
    - Add sticky headers (or simple text items) for each date group to reduce repetition within cards.
    - Simplified header: "32 Check-ins" next to the screen title.
  </action>
  <verify>Verify logs are visually grouped by date, making it easier to scan.</verify>
  <done>Logs are organized into logical date sections.</done>
</task>

<task type="auto">
  <name>Simplify WorkLogCard (Minimalist Style)</name>
  <files>android/app/src/main/java/com/gigone/saarthi/ui/screens/WorkLogsScreen.kt</files>
  <action>
    - Remove timeline dots and vertical lines.
    - Redesign the card to be more horizontal: [Icon] [Job Name + Time] [Mood Dot].
    - Use a very subtle border (`AppColors.BorderSubtle`) and high contrast text.
    - Make the mood indicator a small colored circle next to the job name or a tiny chip.
  </action>
  <verify>Check that the card looks clean, lacks visual clutter, and uses standard list patterns.</verify>
  <done>Cards are simplified and scan-friendly.</done>
</task>

<task type="auto">
  <name>Polish Bottom Sheet & Transitions</name>
  <files>android/app/src/main/java/com/gigone/saarthi/ui/screens/WorkLogsScreen.kt</files>
  <action>
    - Ensure the BottomSheet remains but with slightly better formatting for the chat bubbles.
    - Add a "Delete Log" option if possible, or just keep it simple as requested.
  </action>
  <verify>Verify the interaction is smooth and the visual style is consistent with the simplified list.</verify>
  <done>Log details are easy to read in the sheet.</done>
</task>

## Success Criteria
- [ ] Shift history is grouped by date (Today/Yesterday/etc).
- [ ] Timeline visual noise (lines/dots) is removed.
- [ ] Cards are compact and minimalist.
- [ ] Overall UI feels "simple and easy" as per user request.
