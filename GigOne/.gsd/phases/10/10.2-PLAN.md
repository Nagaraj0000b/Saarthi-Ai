---
phase: 10
plan: 2
wave: 2
---

# Plan 10.2: ML Microservice Refactor (FastAPI)

## Objective
Update the FastAPI `main.py` serving the XGBoost model to accept 8 boolean skill flags natively from incoming requests instead of the legacy `vehicle` string.

## Context
- ml_engine/main.py
- .gsd/DECISIONS.md

## Tasks

<task type="auto">
  <name>Refactor XGBoost Inference Endpoint Schema</name>
  <files>ml_engine/main.py</files>
  <action>
    - Update `WorkerContext` Pydantic BaseModel by deleting `vehicle: str` and replacing it with the 8 new booleans (`skill_bike: int`, `skill_car: int`, `skill_heavy: int`, `skill_clean: int`, `skill_trade: int`, `skill_care: int`, `skill_digital: int`, `skill_support: int`). Default them to 0.
    - Remove processing logic that checked if `vehicle in COMPATIBILITY[job]` and replace with the new rules (e.g., `skill_bike == 1` for Swiggy, etc).
    - Update the `pd.DataFrame` construction to pass these 8 columns directly instead of encoding a vehicle string.
  </action>
  <verify>curl locally or run FastAPI tests: `python -m pytest ml_engine/test_api.py` if available, or start uvicorn and health check it.</verify>
  <done>FastAPI boots successfully without throwing ValueError on missing vehicle encoders, correctly generating compatibility penalties driven by the skills mapping.</done>
</task>

## Success Criteria
- [ ] Model starts properly with new inputs.
- [ ] Fallback and inference logic respects the new boolean inputs natively.
- [ ] No remnant references to "vehicle".
