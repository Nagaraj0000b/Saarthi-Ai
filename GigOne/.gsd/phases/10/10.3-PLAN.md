---
phase: 10
plan: 3
wave: 3
---

# Plan 10.3: Node Backend Compatibility (Skills Integration)

## Objective
Update the `jobRecommendationService.js` and `jobController.js` to correctly capture a user's `skill_set` from context (or default properties if not available) and route it properly into the ML endpoint request payload.

## Context
- server/services/jobRecommendationService.js
- server/controllers/jobController.js
- (Any other server-side file generating ml context)

## Tasks

<task type="auto">
  <name>Refactor Recommendation Engine Input Generation</name>
  <files>server/services/jobRecommendationService.js</files>
  <action>
    - Ensure Node handles the translation of the user's `skills` array into the 8 specific boolean fields required by the ML Microservice. 
    - E.g. If the user object `user.skills` includes "Bike & Scooter Driving", set `skill_bike: 1`. 
    - Strip references to `user.vehicle` completely from the outbound fetch payload.
    - Validate the fallback rule engine. If ML goes down, the fallback rule should use the mapped skills instead of the user vehicle.
  </action>
  <verify>Run the unit tests (if any) or manually trigger the `/recommendations` API from the frontend/Postman and observe standard logs to ensure skills are sent properly.</verify>
  <done>Console output verifies a correctly shaped payload containing the 8 skills properties sent via fetch API over HTTP to the ML Layer.</done>
</task>

## Success Criteria
- [ ] Backend passes all new properties and correctly maps old references to `user.skills`.
- [ ] No `undefined` variable crashes related to vehicle replacement.
